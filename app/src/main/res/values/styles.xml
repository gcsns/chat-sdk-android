<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style parent="ThemeOverlay.AppCompat.Dark.ActionBar" name="AppTheme.AppBarOverlay"/>
    <style parent="ThemeOverlay.AppCompat.Light" name="AppTheme.PopupOverlay"/>
    <style name="chatTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowSoftInputMode">adjustPan|stateHidden</item>
    </style>
    <style name="textview_message_left">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/average_font_size</item>
        <item name="android:layout_marginLeft">@dimen/margin_average</item>
        <item name="android:layout_marginRight">@dimen/margin_average</item>
        <item name="android:maxWidth">@dimen/max_chat_width</item>
        <item name="android:padding">@dimen/padding_small</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="textview_message_right">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">@dimen/average_font_size</item>
        <item name="android:layout_marginLeft">@dimen/margin_average</item>
        <item name="android:layout_marginRight">@dimen/margin_average</item>
        <item name="android:maxWidth">@dimen/max_chat_width</item>
        <item name="android:padding">@dimen/padding_small</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="info_textviews">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textSize">@dimen/small_font_size</item>
        <item name="android:gravity">center</item>
    </style>
</resources>

